{
  "name": "reactivenotes",
  "version": "0.2.0",
  "description": "Note taking app",
  "author": "Calvin Spealman",
  "license": "MIT",
  "scripts": {
    "test": "node_modules/.bin/gulp test",
    "build": "node_modules/.bin/gulp build",
    "deploy": "node_modules/.bin/gulp deploy",
    "dev": "node_modules/.bin/gulp"
  },
  "devDependencies": {
    "moment": "^2.12.0",
    "plasmiddb": "^1.1.0",
    "reactive-gulp-tasks": "^1.2.0"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "react",
            "es2015"
          ]
        }
      ]
    ]
  }
}
